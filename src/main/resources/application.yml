server:
  port: ${PORT:8080}

spring:
  application:
    name: vgm-backend
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/myapp}
    username: ${DB_USER:postgres}
    password: ${DB_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        temp:
          use_jdbc_metadata_defaults: false
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${GMAIL_USERNAME:}
    password: ${GMAIL_APP_PASSWORD:}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

# メール設定
mail:
  from:
    email: ${MAIL_FROM_EMAIL:noreply@example.com}
    name: ${MAIL_FROM_NAME:VGM Application}

# CORS設定（Cloudflare Pagesからのアクセス許可）
cors:
  allowed-origins: "https://vgm-fe.pages.dev,https://develop.vgm-fe.pages.dev"

# Stripe決済設定
stripe:
  api:
    key: ${STRIPE_API_KEY:sk_test_51Sc1R6LIMKpAk5M0AigQR3rvDqIkDSRw7ujS9qeB0l2rwCk9GejIvdzEhlfIZ1ZrxqgHpEhDBoD6ThYKxdOHJwMD00KqtJvqeQ}
  webhook:
    secret: ${STRIPE_WEBHOOK_SECRET:whsec_your_webhook_secret_here}

# PayPay決済設定
paypay:
  api:
    key: ${PAYPAY_API_KEY:a_iwJP24FrNC_FVKU}
    secret: ${PAYPAY_API_SECRET:VW5JyUKfta0qgiHBw7jZw4G0DaR+gyj0J4TcL+buCK0=}
    baseUrl: ${PAYPAY_API_BASE_URL:https://stg-api.paypay.ne.jp}  # Staging: stg-api.paypay.ne.jp, Production: api.paypay.ne.jp
  merchant:
    id: ${PAYPAY_MERCHANT_ID:your_merchant_id_here}

# マーケットプレイス設定
marketplace:
  platform-fee-rate: ${PLATFORM_FEE_RATE:0.10}  # プラットフォーム手数料率（デフォルト10%）